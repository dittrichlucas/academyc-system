{
    "name": "default",
    "type": "postgres",
    "host": "localhost",
    "port": "5432",
    "username": "postgres",
    "password": "123456",
    "database": "postgres",
    "schema": "public",
    "synchronize": true,
    "logging": ["error", "warn", "log"],
    "entities": [
        "src/models/**/*.ts"
    ],
    "migrations": [
        "migrations/*.ts"
    ],
    "subscribers": [
        "src/subscribers/*.ts"
    ],
    "cli": {
        "entitiesDir": "src/models",
        "migrationsDir": "./migrations"
    }
}
